interface:
  display_name: "Voicebox"
  short_description: "Repo-specific patterns and workflows for voicebox"
  default_prompt: "Use the voicebox repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true